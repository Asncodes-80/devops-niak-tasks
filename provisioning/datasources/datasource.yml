apiVersion: 1

datasources:
- name: My Customized Datasource
  type: postgres
  access: proxy
  orgId: 1
  url:
  user:
  database: postgres
  allowUiUpdates: true
  isDefault: true
  jsonData:
    sslmode: "disable"
    postgresVersion: 1200
    timescaledb: false
  secureJsonData:
    password: ""
version: 1
editable: true